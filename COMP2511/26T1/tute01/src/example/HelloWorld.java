package example; // the package is like the directory of the file

// all code (excluding imports) has to exist within this public class HelloWorld code block
public class HelloWorld {
    public static void main(String[] args) { // type "main" then press TAB for a shortcut
        System.out.println("Hello World!"); // type "sout" then press TAB for a shortcut
    }
}
